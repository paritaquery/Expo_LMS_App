export const APP_CONFIG = {
  name: 'Mini LMS',
  tagline: 'Secure auth persistence is ready.',
  description:
    'Successful login and registration now store the authenticated session in Expo SecureStore and mirror it into the app auth state.',
} as const;
